export default () => {
  const cars:any = []
  const makes = cars.map((car:any) => car.make)

//   // 命令式
/*
var authenticate = function(form) {
  var user = toUser(form);
  return logIn(user);
};
*/

// // 声明式
// var authenticate = compose(logIn, toUser);
}